<div class="nav-search-container">
  <% if current_user.group == "developer" %>
    <div class="welcome-msg">Welcome back <%= current_user.first_name %>, find your next project!</div>
  <% elsif current_user.group == "ngo" %>
    <div class="welcome-msg">Welcome back <%= current_user.first_name %>!</div>
  <% end %>
  <!-- ORGANIZATION AND LOCATION HERE -->
  <div class="search-input-group">

    <div class="nav-search-item">

      <!-- KEYWORD SEARCH -->
     <!--  <div class="search-label text-white-left">KEYWORDS:</div> -->
        <%= form_tag("/home", method: "get", class: "") do %>
          <div class="search-box">
            <%= text_field_tag :query, params[:query],
                          class: "nav-search-field ",
                          placeholder: "search" %>
            <%= button_tag(type: "search", class: "btn btn-default") do %>
              <i class="fa fa-search white-icon"></i>
            <% end %>
          </div>
        <% end %>


      </div>
  </div>

      <div class="white-logo">DevForGood</div>

  <!-- ANY FILTERS HERE -->
</div>
