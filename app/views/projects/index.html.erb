<div class="section-container" id="project-index-container">
  <h2>Discover our exiting projects!</h2>
  <div class="cards-project" >
    <% @projects.each do |project| %>
      <%= render 'shared/cards/project_card', project: project %>
    <% end %>
  </div>
</div>
