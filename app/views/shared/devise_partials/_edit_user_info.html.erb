<%= simple_form_for(current_user, url: user_registration_path, html: { method: :patch }) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :email, required: true, autofocus: true %>

      <%= f.input :first_name,
                  required: false %>
      <%= f.input :last_name,
                  required: false %>
      <%= f.input :job_title,
                  required: false %>
      <%= f.input :contact_number,
                  required: false %>
      <%= f.input :facebook,
                  required: false %>
      <%= f.input :twitter,
                  required: false %>
      <%= f.input :instagram,
                  required: false %>
      <%= f.input :linkedin,
                  required: false %>
      <%= f.input :github,
                  required: false %>
      <%= f.input :bio,
                  hint: "Bio: Tell us about yourself.",
                  required: false %>
      <%= f.input :interests,
                  hint: "Interests: Tell us about your professional interests.",
                  required: false %>
      <%= f.input :experience,
                  hint: "Experience: Tell us about your professional experience.",
                  required: false %>
      <%= f.input :skills,
                  hint: "Programming languages and platforms skills",
                  required: false %>
      <%= f.input :photo,
                  hint: "Upload a photo of yourself.",
                  required: false %>
      <%= f.input :hibernate,
                  hint: "Hibernate your account.",
                  required: false %>
      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
      <% end %>

      <%= f.input :password,
                  hint: "Leave blank if unchanged",
                  required: false,
                  input_html: { autocomplete: "New-password" } %>
      <%= f.input :password_confirmation,
                  required: false,
                  input_html: { autocomplete: "New-password" } %>
      <%= f.input :current_password,
                  hint: "Please enter your password to confirm changes",
                  required: true,
                  input_html: { autocomplete: "current-password" } %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Update", class: "teal-button" %>
    </div>
  <% end %>


  <div class="general-text cancel">Unhappy? <%= link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></div>
