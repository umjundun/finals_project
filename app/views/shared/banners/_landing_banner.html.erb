<div class="banner-background" id="landing-banner">
  <div class="banner-text">
    <div class="landing-banner-title">DevForGood</div>
      <div class="landing-banner-subtitle"><span>Work on projects that matter.</span></div>
      <div class="signup-calltoaction">
        <% unless current_user.present? %>
           <a href="#" class="yellow-button" id="modal-signup-button">
             <%= render "shared/devise_partials/modal_sign_up" %>
             Sign Up
           </a>
        <% end %>
     </div>
  </div>
</div>
