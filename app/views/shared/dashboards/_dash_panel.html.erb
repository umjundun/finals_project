<div class="nav-search-container">

  <!-- ORGANIZATION AND LOCATION HERE -->
  <div class="search-input-group">

    <div class="nav-search-item">

      <!-- KEYWORD SEARCH -->
      <div class="search-label">KEYWORDS:</div>
        <%= form_tag("/home", method: "get", class: "") do %>
          <div class="search-box">
            <%= text_field_tag :query, params[:query],
                          class: "nav-search-field ",
                          placeholder: "Find your next project" %>
            <%= button_tag(type: "search", class: "btn btn-default") do %>
              <i class="fa fa-search white-icon"></i>
            <% end %>
          </div>
        <% end %>
      </div>
  </div>

  <!-- DASHBOARD BUTTONS HERE -->
  <div class="search-filters-group">

    <div class="dash-nav-links" id="dash-profile-button">
      <i class="fa fa-user-circle"></i>
      <p>Profile</p>
    </div>

    <% if @user.group == "ngo" %>
      <div class="dash-nav-links" id="dash-projects-button">
        <i class="fa fa-pencil-square-o"></i>
        <p>My Projects</p>
      </div>
    <% end %>

    <div class="dash-nav-links" id="dash-engagements-button">
      <i class="fa fa-pencil-square-o"></i>
      <p>Engagements</p>
    </div>

    <div class="dash-nav-links" id="dash-settings-button">
      <i class="fa fa-wrench"></i>
      <p>Settings</p>
    </div>

  </div>
</div>
