<%= link_to project_path(project) do %>
  <div class="card-project">
    <img src="<%= project.organization.logo %>"/>
    <div class="card-project-infos">
      <h2><%= project.title %></h2>
      <p><strong><%= project.description %></strong></p>
    </div>
  </div>
<% end %>
