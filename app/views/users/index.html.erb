<div class="subsection-title text-center mt-4">Meet our Devs :)</div>
<div class="container d-flex justify-content-around py-4">
  <div class="row" >
    <% @users.each do |user| %>
      <% if user.organization_id == 1 %>
        <div class="card-project mb-3 mr-1 ml-1">
          <img class="avatar-large" alt="avatar-large" src="<%= user.photo %>" />
          <div class="card-project-infos">
            <h2><%= link_to user.first_name, user_path(user) %></h2>
          </div>
        </div>
      <% end %>
    <% end %>

    <%= pie_chart User.group(:username).count %>

  </div>
</div>
