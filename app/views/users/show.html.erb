<% if @user.group == "developer" || @user.group == "ngo" %>
  <%= render 'shared/dashboards/dashboard', user: @user %>
<% else %>
  Dashboards are not currently enabled for <%= @user.group %>s.
<% end %>

<%= javascript_include_tag 'dashboard' %>
